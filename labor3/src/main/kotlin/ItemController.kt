//class ItemController(private val itemService: ItemService) {
//    fun quiz(numQuestions: Int) {
//        val randomItems = itemService.getRandomItems(numQuestions)
//
//        // Implement the logic to perform the quiz
//        // Show questions one by one, request user's response, evaluate the response,
//        // and show the final result in the specified format
//    }
//}